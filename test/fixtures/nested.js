/**
 * Code below must contain the useful comment
 */
function foo() {

	/**
	 * Uh oh. Don't stop me now bro, I'm on a roll!
	 */
	var pooh = 'bear';

  // Maybe useful
  doSomething();
  if(true) {
  	blah();
  }
}

function bar() {
	doSomethingElse();
}

/**
 * Wowsa this is super useful.
 */
var foo = 'bar';

foo = 'baz'; // oh dear